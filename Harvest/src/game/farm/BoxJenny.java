package game.farm;

import game.mainScreen.ImageButton;
import guiTeacher.components.Action;

public class BoxJenny extends ImageButton {

	public BoxJenny(int x, int y, int w, int h, String imageAddress, Action action) {
		super(x, y, w, h, imageAddress, action);
	}
}
